---
interface Props { eyebrow?:string; title:string; lead:string; } const {eyebrow,title,lead}=Astro.props;
---
<section class="page-hero shell"><div class="narrow">{eyebrow&&<p class="eyebrow">{eyebrow}</p>}<h1>{title}</h1><p class="lead">{lead}</p><slot/></div></section>
